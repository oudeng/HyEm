Raw datasets are excluded from this repository due to file size constraints (10+ MB). Use our download scripts to fetch datasets directly from official sources to `data/raw/`:
```bash
python scripts/00_download_data.py --datasets hpo do
```
